spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848/
      config:
        server-addr: http://localhost:8848/
        file-extension: yaml
  application:
    name: cloud-nacos-config
server:
  port: 3377