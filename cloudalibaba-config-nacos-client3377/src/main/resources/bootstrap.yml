server:
  port: 3377
spring:
  application:
    name: cloud-nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848/
      config:
        server-addr: http://localhost:8848/
        file-extension: yaml

